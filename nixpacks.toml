[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt", "python -m playwright install --with-deps chromium"]

[start]
cmd = "uvicorn job_agent.web_app:app --host 0.0.0.0 --port ${PORT:-8000}"
